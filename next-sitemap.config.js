/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://karmcherry.web.app', // ✅ Your live site URL
  generateRobotsTxt: true, // ✅ Also generate robots.txt
  sitemapSize: 7000, // Optional: split sitemaps if needed
};
